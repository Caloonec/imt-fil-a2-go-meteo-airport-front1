export class MeasureList {
    constructor(
        public values: number[],
        public labels: string[], 
        public mesureType: string,
        public color: string,
        public title: string
    ) {}
}